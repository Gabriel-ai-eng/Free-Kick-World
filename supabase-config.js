// Config do Supabase para o jogo (window.__ALPS_SB__).
// Os valores (URL + anon key) são PÚBLICOS e protegidos por RLS no banco.
// Deixe vazio para rodar o jogo sem persistência (salvar/retomar desativado);
// preencha com a URL + anon key do projeto Supabase para habilitar o salvamento.
window.__ALPS_SB__ = { url: '', anonKey: '' };
